# 数据库基本配置
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=your_database
DB_SCHEMA=your_schema

# 数据库连接池配置
DB_POOL_MAX=20
DB_POOL_MIN=5
DB_CONNECTION_TIMEOUT=5000
DB_IDLE_TIMEOUT=30000

# 数据库重试配置
DB_RETRY_ATTEMPTS=3
DB_RETRY_DELAY=1000

# 数据库超时配置
DB_CONNECT_TIMEOUT=10000
DB_STATEMENT_TIMEOUT=30000
DB_LOCK_TIMEOUT=10000

# 数据库日志配置
DB_SLOW_QUERY_THRESHOLD=1000

# 数据库缓存配置
DB_CACHE_DURATION=60000

# 服务器配置
PORT=3300
NODE_ENV=development

# 说明：
# 1. 复制此文件并重命名为 .env.development 用于开发环境
# 2. 复制此文件并重命名为 .env.production 用于生产环境
# 3. 根据实际环境修改相应的值
# 4. 不要将包含实际密码的 .env.development 或 .env.production 提交到版本控制系统